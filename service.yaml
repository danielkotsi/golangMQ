apiVersion: v1
kind: Service
metadata:
  name: my-broker
spec:
  type: NodePort


  selector:
    app: my-broker


  ports:
    - protocol: TCP
      port: 5672
      targetPort: 5672
      nodePort: 30067
